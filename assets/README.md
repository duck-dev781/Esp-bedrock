# Runtime assets

The server can load small original assets/data files from the SD card.

Minecraft clients normally render the graphics themselves, so the server does not need to stream copyrighted Minecraft textures to players.

Use this directory for:

- original block metadata
- server icons
- small generated data tables
- optional original resource-pack experiments

Large world data belongs on the SD card, not in firmware.
